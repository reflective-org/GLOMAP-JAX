# outputs

Scratch for run products: CSVs from the reference, dumps, captured `.npz`
before they are blessed. Everything here is gitignored.

Committed reference output lives in `tests/goldens/`, guarded by
`MANIFEST.json`. Nothing in this directory is a golden, and nothing here should
be compared against.
